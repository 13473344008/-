// Independent fallback: a failed module load cannot leave an infinite spinner.
(()=>{const node=document.getElementById('boot-state');if(node){node.querySelector('h1').textContent='Loading published passport…';node.querySelector('p').textContent='Reading the published record.';node.setAttribute('aria-busy','true');}setTimeout(()=>{const node=document.getElementById('boot-state');if(node){node.querySelector('h1').textContent='Passport temporarily unavailable';node.querySelector('p').textContent='Please reload this page or try again later.';node.setAttribute('aria-busy','false');}},12000);})();
